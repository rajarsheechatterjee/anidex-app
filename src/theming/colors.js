import { Colors } from "react-native/Libraries/NewAppScreen";

const colors = {
    headerColor: "#3b97fc",
    headerText: "white",
    headerSubtitle: "#E0E0E0",
    headerIcon: "white",
};

export default colors;
